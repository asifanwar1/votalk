import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    featureItem: {
        alignItems: "center",
        flex: 1,
    },
    featureIcon: {
        width: 48,
        height: 48,
        borderRadius: 12,
        alignItems: "center",
        justifyContent: "center",
        marginBottom: 8,
    },
    featureText: {
        fontSize: 12,
        color: "#64748b",
        fontWeight: "500",
        textAlign: "center",
    },
});
